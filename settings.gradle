file('UTs').eachDir { sub ->
    if (file("UTs/$sub.name/build.gradle").exists()) {
        println("Adding project $sub.name")
        include ":$sub.name"
        project(":$sub.name").projectDir = new File("UTs/$sub.name")
    }
}